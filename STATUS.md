# STATUS — in flight

Agent radar for multiplayer work. **Update this when you claim, block, or finish.**  
Process: [`docs/collab.md`](docs/collab.md) · Issues/PRs on GitHub.

## In flight

| State | Issue | Owner | Branch | Plan | Hot files | Notes |
|---|---|---|---|---|---|---|
| In review | #586 | Tai | `cursor/fix-duplicate-atoms-plus-byok-d208` | (debug: body+source capture identity; Plus key is usage fallback) | `src/pipeline/render.ts`, `src/pipeline/write.ts`, `src/pipeline/backfill.ts`, `src/platform/filingAuth.ts`, `src/settings/settings.ts`, `test/collision-integrity-repro.test.ts`, `test/render.test.ts`, `test/write.test.ts`, `test/filingAuth.test.ts`, `test/settings.test.ts` | Plus+BYOK does not dual-fire one Process. Plus first while it can classify; stored key files only after Plus is exhausted. Identity is body+source daily. Residual: both devices can still race before either atom syncs. |
| Blocked | #579 | Tai | `cursor/plan-sidebar-header-4de5` | `docs/plans/2026-08-19-001-fix-persistent-sidebar-header-plan.md` | `src/home/atomsHomeView.ts`, `test/atomsHomeView.test.ts`, `test/helpers/homeView.ts`, `tsconfig.test.json`, `package.json`, `manifest.json`, `versions.json`, `docs/solutions/ui-bugs/shared-chrome-must-render-before-detail-branches.md`, `docs/qa/2026-08-19-persistent-sidebar-header-world-class-qa.md` | Code + automated checks complete. Merge-readiness blocked: Obsidian CLI and shared world-class/adversarial QA skills unavailable; live sidebar smoke and screenshots pending. |
| In progress | #571 | Tai | `feat/watchlist-hub-membership` | `docs/plans/2026-08-17-2132-fix-watchlist-hub-membership-plan.md` | `src/pipeline/enrich/media.ts`, `src/pipeline/enrich/listHubs.ts`, `src/pipeline/hubInvite.ts` | Watchlist membership = named work + family on the work. Draft. |
| In progress | #561 | Tai | `feat/561-android-capture-live` | (light: Play live + API 36) | `companion/android/app/build.gradle.kts`, `mobile-install.json`, `src/shared/mobileInstall.ts`, `src/settings/settings.ts`, `www/src/setup.html.tmpl` | Atoms Capture is on Play. Target API 36. Plugin + /setup point Android readers at the app, not "no equivalent yet." |

## How to claim (copy)

1. Create/assign GitHub Issue  
2. `git checkout master && git pull && git checkout -b feat/<name>`  
3. Add a row above (State = `In progress`)  
4. Push branch and open **draft** PR  
5. Only then implement  

States: `Queued` · `In progress` · `Blocked` · `In review` · `Done` (then remove row)

## Recently merged (optional, last ~5)

| Merged | Issue / PR | Summary |
|---|---|---|
| 2026-08-22 | #596 / #595 | Update notes once on Home, then Settings · **0.8.16**. Home names the quality once (`Update notes` + q9 reason, no count). Confirm is spend-only. Not now or `updated > 0` hides Home until the next CURRENT. More waves live on Settings File `Update notes › Readings can link`. Ranker: source daily then `created`, never mtime. Plus leftover key stays Plus on Update (`projectPlusIdentityAuth`). Report: `docs/qa/2026-08-21-update-notes-once-then-settings-world-class-qa.md`. Paid wave / leftover-key Modal not proven live. |
| 2026-08-21 | #589 / #590 | Measurement series · **0.8.15**. A reading of a durable thing is an atom, not noise (both prompt surfaces, parity-locked); return intents open loops; series links + `Track My car?` at reading two; Home loop-close offer (both captures verbatim, no arithmetic); Keep as note runs the enrichment chain; quality 8→9 so Update notes repairs pre-feature readings. Full tail ran remotely (high-effort code review: 7 findings fixed incl. a body-is-sacred P0 in `applyHardLinkToAtomContent`; 4-agent simplify). **Owner-merged with live evidence gate waived** — post-merge: owner Update notes run on the real 73042/73089 pair, vault screenshots, Fly deploy for Plus prompt. Report: `docs/qa/2026-08-21-feat-measurement-series-world-class-qa.md`. |
| 2026-08-20 | #583 / #584 | Together occupies Home only as news that something joined a hub · **0.8.13**. Directory dump gone. Open goes to the hub note. First catalog fill is the existing hub-list preview. Report: `docs/qa/2026-08-20-feat-together-news-hub-catalog-world-class-qa.md`. |
| 2026-08-20 | #580 / #581 | Do not name an atom after an unrelated context title · **0.8.12**. Post-classify repair rewrites a neighbour paraphrase (iPhone capture titled as farmers-carries) from the capture; Continue parent is a neighbour on live and fixture Process. Exact-title #66 body-gate unchanged. [The learning](docs/solutions/logic-errors/paraphrased-context-title-is-still-foreign.md). **Owner-merged; live Process and world-class-qa not run.** Existing poisoned markers need a rename. |
| 2026-08-18 | #573 / #577 | Ask `list_atoms` gains a derived `open_now` filter (active loop ∧ no redeeming child) so a scheduled morning brief can list open loops without paging the whole mirror. One home in `paginateMirrorList`; all three stores flow through it; redeeming-child exclusion mutation-checked. Server-only, no plugin bump. **Fly deploy needed before live.** |
| 2026-08-18 | #574 / #576 | Ask instructions: **a reminder created elsewhere is not a close** — record an ordinary continue child, never `redeems`, never `set_loop resolved_elsewhere`; the loop stays open until the thing itself happens. Parity test locks the sentence. Companion runbook `docs/runbooks/daily-atoms-brief.md` (#575): scheduled-brief recipe, read-only on loop state, parked-child protocol with the pending-counts-as-parked guard. **Fly deploy needed before live.** |
| 2026-08-17 | #566 / #567 | One Home invite for every hub association · **0.8.8**. After Process, Home asks **Add to Show list?** (same card family as people). Accept writes `- [[atom]]` in the marked hub block; handwritten checkboxes stay. Confirm once, then stay quiet. **Owner-merged with silent landing unproven live** — Plus dogfood filed Frieren with show tags and no `[[Show list]]`; matcher fix is unit-tested only. Wait card still hides the invite while a backlog sits in the hero. Report: `docs/qa/2026-08-17-hub-association-invite-world-class-qa.md`. |
| 2026-08-17 | #536 / #549 | A proxy 401 is a refusal, not offline · **0.8.4**. The issuer gate collapsed every non-`auth` 401 into `unreachable` and the offline sentence, so a gateway / WAF refusal never wrote a recovery row. Three outcomes now: session-shaped stays `unverified`; any other 401/403 is `upstream` and recorded; 5xx and transport stay unreachable. [The learning](docs/solutions/logic-errors/a-401-that-is-not-about-the-session-is-still-a-refusal.md). |
| 2026-08-17 | #538 / #543 | Get started uses the File row's noun · **0.8.3-beta.2**. Unsigned Settings had two names for one unfinished step: Get started said `Choose who files your captures`, File said `Filing`. `FILING_NAME` is the one string now. |
| 2026-08-16 | #508 / #526 | A Plus session records the base that issued it · **0.8.3-beta.1**. Clearing `Plus service URL` used to send a self-hoster's token *and their verbatim capture text* to the hosted service; #500 guarded invalid bases and this closes the one input class that is not invalid at all, absent. The session now carries `issuedBase`, branded so it can only come from a response, and a resolved base that disagrees refuses classify and mirror push until a content-free `/v1/me` accepts the token. An absent stamp means unknown, never production, and unknown **plus an empty field** refuses without probing at all -- otherwise the first act on upgrade would be to send the self-hoster's token to exactly the host this issue exists to protect them from. **QA found five defects unit tests had not**, two of them only in the adversarial half after the happy-path pass went 10 for 10: a 401 from a base that never issued the session offered a sign-in link *at that base*, whose redemption stamps the hostile host as issuer with the gate never running (the branch's own R1 comment already refuses that door for the renamed-account case and explains why; the 401 branch had never been told); a stale `rejected` record survived a re-link and, after an account change, mailed a link to the departed address; the mismatch state reported `Plus · 50 filings left` while every Process refused; the recovery row vanished after a typo; and a Notice read `Atoms Plus: Atoms Plus couldn't save your plan`. All fixed here, each neuter-verified against a named test -- including one neuter that silently failed to substitute and produced a vacuous pass, now a learnings row. 2044 -> 2104 tests. **Residual filed:** [#536](https://github.com/taihartman/obsidian-atoms/issues/536), a proxy 401 reads as "you're offline" forever and never reaches the new row; fails closed. Report: `docs/qa/2026-08-16-claude-plus-session-issuer-world-class-qa.md`. |
| 2026-08-16 | #533 / #534 | Back out of a settings destination and you are where you left, not at the top · **0.8.2**. `openRoute` serves both directions and ended in an unconditional `scrollTop = 0`. Its own comment gave the reason and the reason holds for exactly one of them: a destination is a screen you have not seen, so it starts at the top. The back row calls `openRoute("main")`, and the main screen is the one screen you have seen and left partway down. Position is now kept **per route**, so the rule follows the screen rather than the direction, keyed rather than held as a single previous value that would go wrong silently the moment a destination led to another. **Present since #493 and surfaced by 0.8.1**, which put the Atoms Plus group at the bottom of the main screen and so made Account and Redeem code the two rows worth returning from. **The `hide()` clear took three tries to test honestly:** the first two drafts passed against a build with the clear deleted, because the main screen is re-recorded every time it is left, so a stale value *there* is always overwritten before it could be restored. Only a destination's can outlive a visit, and the test now walks that far. All four tests mutation-checked. 1967 tests, lint and build green. **No device pass** — scroll restoration is motion, so no screenshot could have shown it either way. |
| 2026-08-16 | #530 / #531 | The filing decision reads as a noun, and a promo code has somewhere to go · **0.8.1**. `Who does the filing` asked a question in a row label, and three rewrites of that question all felt like improvements and none finished, because the *form* was wrong: an iOS settings row names a thing and puts its condition in the answer. `Filing › Not set up`, with the word defined once in the File group footer. The screen was also two things at once, a decision and a credential form, so the key field, the device-local fallback and the secret-id naming rule moved behind `Use your own Anthropic key` — **all eight** undefined terms off the decision screen with nothing deleted. `Pick one` retires into `Recommended` / `Instead`, because a recommended path and an escape hatch are not peers. **The promo half was already shipped and merely unfindable** (`allow_promotion_codes` has been on the whole time); one `Redeem code` row now sits in the Plus group and in a new main-screen `Atoms Plus` group, since `openRoute("account")` had exactly one call site and billing had no presence on the main screen at all. **The bug worth remembering was caught in design, not review:** hiding that row on a live subscription to avoid buying a second one took it away from a *trialing* reader, because `start_trial` creates a real Stripe subscription — the likeliest holder of a code, left with no route and no explanation. A hidden affordance is worse than a dead end. The row never hides; the destination branches on `hasLiveSubscription`. Two Apple patterns deliberately **refused**: silence about the vendor, and moving `What gets sent` behind a linked sheet (it previews a versioned, acknowledged disclosure). **Merged on the owner's explicit call with the UI evidence gate unmet** — no live-vault screenshots and no device pass, the authoring environment having no Obsidian CLI or vault, and no multi-persona doc-review (those skills are not installed there). 1964 tests, lint and build green. **Two open risks, both live:** the Stripe portal's promotion-code configuration is unverified, so `active` and `exhausted` may reach a portal with no code field; and the settings screens have never been seen on a device, which is exactly what caught a P1 on #493. [The learning](docs/solutions/ui-patterns/a-settings-row-is-a-noun-and-its-state-is-the-answer.md). |
| 2026-08-15 | #364 / #525 | The settings header has something to sit on · **0.8.0-beta.7**. Obsidian floats it over the scrolling content behind a scrim masked by `linear-gradient(black 20%, transparent)` — 27.8px of a 139px scrim — while the title sits at 70-91px and the circles reach 103px, so rows read through the title. Recorded twice (#364 C1, and F9 in the #493 report) and confirmed on a real Android WebView. The stop is now 80%, a **percentage not a pixel**, because the header is `safe-area-inset-top + 48` so the fraction that clears the circles moves with the notch. Scoped to a marker class, since the header is chrome every settings tab shares. **The first version leaked and device QA caught it:** Obsidian detaches `containerEl` *before* `hide()`, so the removal no-opped and the reused modal carried our scrim onto every other plugin's tab for the session — the test had encoded the wrong teardown order and passed throughout. C2 was already handled by #493's R17, so the issue is closed. |
| 2026-08-15 | #516 / #517 | One writer at a time on the shared QA vault. Four sessions drove `test_vault/test vault` through the same Obsidian in a morning, and a peer logged an install landing 0.8.0 at 09:19, confirmed at 09:21, overwritten with 0.7.12 by 09:23 — both builds naming a plausible version, so a version read-back does not catch it. `install-to-vault.sh` now takes the lock itself and refuses with exit 3 when another **worktree** holds it (worktree, not process, so a session's subagents share its claim); stale at 45 minutes so an abandoned session cannot wedge the machine; reads stay unguarded because `obsidian eval` has no isolation and only the write corrupts a pass. **It also fixed an older bug worth more than the lock:** `install-to-vault.sh` defaulted to the *running worktree's* vault, so each worktree minted its own and installed there while the CLI answered for the main one — the `REFUSING to report success` line. Docs: `docs/qa/README.md` § Vault lock. |
| 2026-08-15 | #509 / #510 | Plus network failures say what to do instead of naming the browser API that gave up · **0.8.0-beta.3**. `Atoms Plus: Plus network error (TypeError: Failed to fetch)` became `Could not reach the Plus service. Check your connection and try again.` The thrown shape rides on a new `PlusApiError.detail`, redacted and documented as diagnostics only, because there is no console sink in `src/` (the community scan forbids `console`). Found as H4 in the #494 adversarial pass. Composes with #500: a refused base is refused before any request, so only an allowed but dead base reaches this message, verified live on both. |
| 2026-08-15 | #500 / #502 | The Plus service URL took any scheme and any host while every call attached the device session token, and `/v1/classify` attached the user's verbatim capture text · **0.8.0-beta.2**. Empty is the hosted default, `https://` anywhere, `http://` only on loopback, everything else refused, enforced in `plusRequest` so a bad base never sends the token, and refused rather than silently swapped for production. Rebasing it onto the new settings screen also surfaced a defect in the overhaul: the group separator was `.setting-item + .setting-item`, so the first non-row element to render inside a group, this PR's error line, silently took the hairline off the row beneath it. `~` now. |
| 2026-08-15 | #493 / #494 | Settings regrouped around the product's three legs · **0.8.0-beta.1** (prerelease, betas-enabled BRAT only). A status group answers "is Atoms filing" before any control, explanation moved from rows into one footer per group, and consent records, diagnostics and the escape hatches moved to destinations. Signed-out root at 390px went **3,444px → 2,121px** and **30 → 11** undefined terms. Two P1s were found by looking at frames rather than asserting CSS: Obsidian gives every mobile `.setting-item` its own opaque card, so the group fill, corners and hairlines were painting behind floating pills while the computed-style check passed; and three text rows collapsed at `is-tablet`, where the pane is 489px and the group inside it 410px, so window-width media queries fire on the wrong thing. The adversarial gate then found the one that mattered: a device whose key had been deleted still read "Atoms files each past day when Obsidian opens" with no engine to send anything, because `hasFiled` consulted the device's own stamps and never `resolveFilingAuth`. **Stable 0.8.0 waits on a real-device look** — F9, the sticky header title overlapping scrolled text, has only ever been seen under desktop mobile emulation. Ten P3s recorded with reasons; [report](docs/qa/2026-08-14-493-settings-three-leg-overhaul-world-class-qa.md). |
| 2026-08-15 | #501 / #503 | `clampAtomFolder` rejects a folder Obsidian will not index · no bump (rode 0.8.0-beta.1). A leading dot was accepted because every existing guard was written for traversal, and Obsidian does not index a dot-folder: atoms filed there are absent from the explorer, from search and from the graph, and the `↳ [[title]]` marker in the daily note can never resolve. Length was unbounded, so a 300-character name made every write throw `ENAMETOOLONG` and filing stopped producing atoms in silence. Both found by the adversarial pass on #493 and live-reproduced. The cap sits at **255 bytes, the filesystem's own limit, deliberately not lower**: this value is applied to the *stored* setting at load, so a stricter rule would relocate a working vault's folder on upgrade and strand the atoms already in it. Review then caught that the repair was memory-only — `data.json` kept syncing the broken name to every other device — and that it happened silently, so it now persists and fires a one-time Notice naming the folder given up. [The learning](docs/solutions/logic-errors/a-guard-that-repairs-a-stored-setting-must-persist-and-confess.md). |
| 2026-08-13 | #486 / #487 | Account Email cluster stacked so name and desc stay readable · **0.7.10** |
| 2026-08-13 | #478 | DIY Ask self-host for non-Plus users · **0.7.8**. Advanced → DIY Ask guide; runbook requires public HTTPS. |
| 2026-08-12 | #473 / #474 | Magic-link sign-in redraws the open Account page · **0.7.7**. `installPlusSession` notifies Settings; `docs/qa/learnings.md` is project QA memory. |
| 2026-08-12 | #471 / #472 | Ask agent stance: search the mirror without “check my atoms” · plus-service only. One look = no `search_atoms` retry; fetch / list_tags / mirror_status still apply. **Fly v63**. New chat after deploy. |
| 2026-08-12 | #467 / #468 | set_loop: mark existing atoms in conversation · **0.7.6**. MCP + outbox FM patch; R15 payload passthrough. Fly v60. |
| 2026-08-12 | #320 / #322 | Multi-device Plus sessions: sign-in no longer boots other devices · **0.7.5**. exchangeMagic revokes unverified only; Sign out all devices for recovery; cap 10. |
| 2026-08-11 | #461 / #462 | Open loops: intention notes are first-class · **0.7.4**. FM `atoms-loop` + sticky source; open-now = active ∧ ¬redeems child (ordinary continues never closes); MCP list/search/fetch project open_now; Process heuristic + Ask flag; Browse/Review commands (no Home guilt shelf). Review fixed list SELECT omitting `loop_json` (always false open_now). Residual: agent dogfood SC1–SC2 needs live Ask+Fly; plus-service deploy for MCP fields. |
| 2026-08-11 | #441 / #452 | A spent filing meter no longer revokes **Ask/MCP** for a paying subscriber — Claude and ChatGPT kept losing a brain that was already mirrored and already paid for · **server-side, deployed to Fly v54** (no plugin bump). One `exhausted` status carried two unrelated meanings — the period **ended** vs this period's allotment **spent** — and **eleven gates across three layers** read it as one. The issue blamed `entitled()`, which is not on the failing path at all (search goes through MCP); the triage doc named two layers, verified line by line, and still missed the seven **OAuth connect** gates that told a subscriber "Plus required" when reconnecting an AI app. `subscriptionLive` is now the one home for the split, and checking the period **before** the status also closed a pre-existing hole where a stale `active` row with a past `periodEnd` could connect an AI app. An ended period still revokes. **Review earned its keep:** three independent reviewers converged on the 8 mirror routes + 7 OAuth gates being verified only by a source scan that the files *mention* the helper — a file can mention it and call it backwards — now driven at route level and mutation-checked both directions; the cross-model pass (Grok) caught a transition the security lens misread. The regression test also *assumed* the meter transition it tested until it was rewired through the real `tryConsumeFiling` UPDATE. **Deliberate and recorded:** Ask writes survive a spent meter (only `/v1/classify` ever charged it), all eight Plus-session routes survive, and a spent **trial** meter keeps reads for the rest of the 14-day trial — owner call, pinned by a test. **Residual, filed nowhere yet:** `unknown` would elevate into Ask by spending its meter (no code writes `unknown`, so unreachable today), and token mint does not re-check the predicate. **Never reproduced live** — no production account has hit the cap; proven at predicate, store (incl. real postgres in CI), and route levels. |
| 2026-08-11 | #446 / #447 | A shortened service message stopped mid-word — the push-failure Notice read "…Sign in again to reconnect **t**", losing the second half of the only instruction it was giving · **0.7.3**. A bare character cut reads as a finished sentence that merely ends badly, so the reader cannot tell anything was removed. **The triage note named the wrong line:** it blamed `formatAskMirrorStatusLine`'s 96-char slice, but the status line renders `Ask mirror:` while the observed Notice begins `Ask:` — the real site was `askCoordinator`'s `.slice(0, 72)`, reproduced byte-for-byte on the 85-character message. The 96-char site has the same defect and simply did not fire on that message, so fixing only what was reported would have left the bug untouched. One `truncateMessage` serves both now: word-boundary cut, dangling punctuation stripped, ellipsis **only** when text was actually removed. A single token longer than the budget is still cut mid-token on purpose — the callers render the clause only when it is non-empty. Live evidence covers the 72-char Notice path; the **96-char path is unit-tested only**, because the real service message is under that budget. Screenshot missed its paired capture (the second `dev:screenshot` hung) — settled frame, but without the confirmation this repo adopted after shipping pre-render evidence. |
| 2026-08-11 | #442 / #443 | An expired trial was described to users as a spent monthly meter, because the service returns one `exhausted` status for both an ended period and a used-up allotment · **0.7.2**. Four rows wrong at once: "Monthly limit reached", a past date labelled "Renews", an offer to buy filings against a billing date that did not exist, and a billing portal for an account with no Stripe customer — while the Ask mirror line reported a healthy cached count, 23 hours after the lapse. The first real signal reached the user on an unrelated button (`Plus entitlement required for Ask pairing`). Root cause confirmed against production before any code. `plusLapse` is the one home for the distinction. **Review caught a P0 in the first cut:** it let the device infer a lapse without server confirmation, which told a *converted* trial subscriber their trial had ended and blocked their filing — where the old code would have called the server and been corrected. A lapse now requires the service to have said `exhausted`; staleness is named (`plusNeedsPeriodRefresh`) and answered by asking. One quiet `/v1/me` at load closes the silence — nothing else announces an expiry, since the only automatic refresh returns early unless a checkout is in flight. Also ships the Subscribe path, which **did not exist**: the service refuses a second trial once `trial_used` is set. Live QA 7/7 ([screenshots](docs/qa/screenshots/442-expired-trial/)); [the learning](docs/solutions/logic-errors/a-device-may-not-assert-an-entitlement-the-server-has-not-confirmed.md). **Not proven:** no live Stripe expiry was exercised. Follow-up **#441** unclaimed — a spent filing meter also revokes Ask/MCP for paying subscribers. |
| 2026-08-11 | #438 / #439 | The backfill gate released its note-list run **before** the confirmed submit, so any catch-up spanning more than one calendar month died after chunk one was already paid for · **0.7.1**. `BackfillConfirmModal` closes *before* it calls `onConfirm`, and the call site patched `onClose` to read a `confirmed` flag its own `onConfirm` sets — one step too late — so it called every confirm a dismissal and ran `run.end()`. Not untidiness: `ContextRun.end()` nulls the corpus, after which `getChunkCandidates` **throws** and `addAtom` **silently no-ops**, while `vaultContext` keeps resolving — which is why chunk one looked healthy. A single-month history survived by accident, because chunk one is reused from the cost gate via `firstChunk` and nothing scores afterwards. The modal owns the dismissal answer now (`onDismiss`, fired from `onClose` only when its own `confirmed` — set *before* `close()` — is false), so there is no flag left for a caller to read at the wrong moment. **Found during #433 and deliberately left in scope there**; #434 then landed the gate's restructure mid-flight, so this rebased onto it and the surviving half also deleted `confirmBackfill`'s microtask deferral, which existed only to dodge this ordering. Regression test mutation-checked before *and* after the rebase — removing the guard reproduces the original `[atoms] context run has ended`. **Not proven live:** the confirmed multi-chunk path needs a real Batch submission and real spend, so the ordering is proven at the level that owns it and no further. **Shipping tail skipped by owner call** — no `ce-code-review`, `ce-compound`, or `world-class-qa`. |
| 2026-08-11 | #433 / #434 | Backfill becomes a visible, priced offer that works on Plus and trial, not BYOK-only · **0.7.0**. `runBackfillFlow` opened with `requireApiKey()`, so the one credential a Plus user does not have gated the one route to their own history — the complement #428 deliberately left stranded. One card, two engines, one gate, bounded per period so a single tap cannot spend the whole allowance; the BYOK command stays unbounded as the escape hatch. **Two rounds of `ce-code-review` fixed six P1s**, three of which let a run exceed, drop, or race what the user confirmed: a confirmed Plus run had **no capture ceiling** (priced at derivation, re-scanned at write, so captures landing in between were filed past the quoted number and into the period reserve — only the server's 402 stopped it); BYOK held **no in-flight flag** across its estimate and confirm window, so a second tap's consented paid submit hit the inner flag take and returned **silently**; and filing exclusion was **one-directional** — Process refusing during a backfill did nothing about a backfill starting during Process, because neither attended pass claimed anything. Three independent reviewers, including a cross-model pass at confidence 100, had to point that out ([the learning](docs/solutions/logic-errors/both-sides-must-claim-and-hold-time-is-its-own-question.md)). Reviewing the fixes also caught a regression they created together: `finishRun` refreshes home while the card flag is still held, so the fresh button was drawn disabled and nothing re-enabled it. Live QA passed 19 checks with the exclusion probes re-run against a **genuine open confirm gate** ([report](docs/qa/2026-08-11-433-backfill-p1-fixes-world-class-qa.md)). **One gap, deliberate:** the `maxCaptures` ceiling sits past the gate and is unproven live — the QA vault's `plus-service` points at production Anthropic and is inert only because the key is unset, so crossing either passes vacuously at 0 filed or spends real money; a unit test confirmed failing before the fix stands in. Follow-ups: **#435** (home reads idle at the gate — wants its own phase, not a widened `"process"`), **#436**, **#437**. Also surfaced, pre-existing: `onload` starts a filing pass, and that pass reports `ok` while filing zero. |
| 2026-08-10 | #427 / #428 | Automatic filing files **forward** — every unattended path (load, hourly interval, resume, manual catch-up) is bounded to a filing window starting the day filing is enabled, so no unattended path can reach history · **0.6.99**. The bound **fails closed**: an absent, malformed, or tampered stamp is re-stamped, never read as "scan everything". One resolved bound reaches both the count and the write **by construction** (`runAutoFilingCycle`) — the drift it prevents is silent, costing vault reads rather than API spend, and would rescan the whole vault hourly forever. Only a device with filing **on** persists the start day; reads go through `readAutoFilingSince` after three reviewers caught home's refresh minting device state and stealing the migration's one-shot stamp. Home carries **two counts** — binding the single count would zero it on every un-enabled device and take the enable affordance with it. **KTD1 reversed by the owner:** the enable tap no longer files today, because `EGRESS_DISCLOSURE` clause (3) says today is never auto-touched and the parity test froze the *words*, not the behavior ([the learning](docs/solutions/security/a-consent-parity-test-that-freezes-words-does-not-freeze-behavior.md)); day one is deliberately silent. Disclosure untouched, ack version **not** bumped — narrowing never needs re-consent. Already-enabled devices are stamped once, pausing in-progress sweeps on purpose. Live QA passed 11/12 stories ([report](docs/qa/2026-08-10-427-auto-filing-window-world-class-qa.md)); the miss is a well-formed far-past stamp still sweeping history — **#429**, accepted residual per KTD6. Follow-ups: **#429**, **#430** (migrated devices lose filing with nothing explaining why), **#431**. Deferred: U5 backfill card, U7 copy/release notes, U8 Plus engine (needs its own doc-review). |
| 2026-08-10 | #408 / #410 | Plus portal clears stale test-mode Stripe customer under live keys (reconnect message; filings kept). Fly redeployed. |
| 2026-08-09 | #397 / #402 | Background Ask syncs converge — the hub planner's stale snapshot copy no longer clobbers the atom planner's freshened hashes, so an edited atom is sent once instead of on every pass · **0.6.92**. Present since #225; invisible from the UI, which reported a healthy "last pushed" either way, and paid for in bandwidth, Plus request volume, and Anthropic spend. Both fixes the issue proposed are wrong and the merge-site comment says why. Proven live against a **rebuilt pre-fix control on the same vault** (six uploads of one unchanged atom versus one) plus a 13-scenario adversarial pass with no holes ([report](docs/qa/2026-08-09-397-hub-hash-clobber-world-class-qa.md)). The suite missed it for eight days because the fake host resolved zero hubs, so the clobbering map was never populated ([the learning](docs/solutions/logic-errors/a-planner-that-returns-a-full-copy-cannot-be-merged-with-a-spread.md)). New instrument: `scripts/qa-mirror-stub.mjs`, the permissive counterpart to the egress sentinel. |
| 2026-08-09 | #393 / #401 | Session install disarms Ask when Plus identity changes (magic-link / paste / trial) · **0.6.91**. Same-account re-auth keeps the baseline; residual mirror email covers a lapsed session with no Sign out row. |
| 2026-08-09 | #372 / #389 | Sign-out disarms the Ask mirror and clears device state before the next account can inherit either · **0.6.90**. Teardown has one home (`disarmAskMirror`); in-flight passes cannot restore the hash baseline; live QA found and fixed a second hole — network revoke used to run *before* disarm, so a slow RTT left the gate open for another upsert chunk. Acks survive on purpose. Follow-ups: #393–#397. Server-side account switch still Blocked (plus-service not deployed). |
| 2026-08-08 | #371 #374 #285 / #385 | Wipe disarms the mirror, and the status line stops advertising one the gate has shut · **0.6.88**. Wipe now turns `askEnabled` off and **persists that before** clearing the push baseline — the old order's crash window recovered into #371 itself (armed mirror, empty baseline; a bare plugin reload put all 407 rows back in four seconds). The status line asks the gate and says `off`, why, and that the cloud copy outlives it; a gated-off mirror also drops the error styling, because consent closing it is not a failure. Acks untouched, no ack-version bump. The egress predicate got **one home** — `askMirrorPermitted()` in `src/shared/askAck.ts` — and review caught that a **pre-existing private copy of the same name** in `settings.ts` still gated the toggle's push: the extraction was verified against its own diff, which is why four reviewers had to find it ([the learning](docs/solutions/workflow-issues/extracting-a-one-home-predicate-does-not-find-the-copy-already-there.md)). QA proved the regression against a local stub — post-wipe reload sent **zero** requests — and 12/12 adversarial cases held. **Not proven:** wipe's server-side deletion, token revocation, and the wipe failure branch all need a live Plus service; the mid-pass upload race is pre-existing and deliberately deferred at `askCoordinator.ts:281`. QA also caught `dev:screenshot` returning **pre-render frames**, which nearly shipped a plausible, entirely wrong evidence set — pairing protocol now in the nav map. Still open: #372, #373, and #284 (server-side sign-out teardown, *not* a duplicate of #372) |
| 2026-08-07 | #166 #369 / #362 | Android companion **Atoms Capture** POC — hub vault link, overlay strip, widget, shade tile, live voice; writes `Atoms System/Inbox.md` (no plugin bump) |
| 2026-08-07 | #339 / #340 | Ask search recall + agent honesty · **0.6.87**. `ASK_PRIVACY_ACK_VERSION` → `2026-08-07` for the seven-clause disclosure naming Anthropic search-expansion egress, so every device re-accepts; `ASK_EXPAND_ENABLED` defaults on. `world-class-qa` + `adversarial-qa` ran live on the test vault — A–F pass, disclosures match their constants byte-for-byte, zero egress without a current ack ([report](docs/qa/2026-08-07-340-ask-expand-world-class-qa.md), 17 screenshots). The adversarial half proved five **pre-existing** holes in the surrounding Ask surface, filed not fixed: #371 (Wipe undoes itself), #372 (consents inherited across Plus accounts), #373 (unresponsive backend wedges the mirror), #374 (status line ignores consent). Still open from this work: **`plus-service` is not deployed to Fly**, so expansion is inert in production whatever the flag says |
| 2026-08-07 | #360 / #361 | Both Ask acks now carry the wording they were granted against, so a reworded disclosure re-prompts instead of riding on an old yes · **0.6.86**. Every gate reads **both halves** — a real, parseable timestamp *and* a matching version — and `revokePaired` forces a version to die with its timestamp even when the incoming payload omits the key. Withdrawal rows stay keyed to the timestamp, so a stale grant keeps the only surface that can revoke it. **Real multi-device Sync was never exercised on hardware** — the mixed-version payloads are reproduced by seeding, and the round-trip they rely on was verified against master's source (`Object.assign({}, DEFAULT_SETTINGS, raw)` in, `saveData(this.settings)` out). Merged on the owner's explicit call, same as #323, with the gap left unchecked in the PR test plan. **Turns [#340](https://github.com/taihartman/obsidian-atoms/pull/340) red on purpose** until it lands the seven-clause disclosure, bumps `ASK_PRIVACY_ACK_VERSION`, adds a **new** keyed `FROZEN_ASK_PRIVACY` entry, and only then flips `ASK_EXPAND_ENABLED`. Follow-ups: #363, #364, #365, #341 |
| 2026-08-06 | #357 / #358 | **Production served a fixture-built site.** `tryatoms-pages.yml` built `www/dist`, then ran the archive test — which rebuilt dist from its fixtures — then deployed that, so the real build never shipped. `ATOMS_DIST_DIR` isolates the test; assertions now run **before** the build so the shipped build is structurally last; committed `www/dist` regenerated (`notes/sample-loop/index.html` was a page for a note that does not exist). **Verified live**: /notes/ shows the empty state, /notes/sample-loop/ 404s. Deleting the source fixture in #355 had changed nothing — the test put it back every deploy |
| 2026-08-06 | #354 / #355 | `npm test` deleted a tracked file every run — the archive test staged its fixture *inside* `docs/field-notes/published/`, which is not empty in git. Build dir is overridable now (`ATOMS_PUBLISHED_DIR`); guarded by an in-suite listing assertion (mutation-tested) **and** a CI clean-tree check that catches the next leak, not just this one. **The stray fixture was live**: tryatoms.app/notes/ listed it as a real Field note with `subject: "Sample Field note"` — removed, so the archive shows its honest empty state. Origin of the whole thing: #335 ran the suite and committed the copy with `git add -A` |
| 2026-08-06 | #323 / #330 | A consent withdrawn on another device closes this device's gate — the egress predicate gets one home, the follow-up mirror pass asks it too, and only a *withdrawal* crosses a lost read/save race, never a grant · **0.6.84**. **The cross-device leg was not reproduced on hardware:** the two-device smoke saw the grant propagate and a restart apply the withdrawal, but never observed a running desktop react. Merged on the owner's explicit call with that recorded in the PR Evidence table — debug ruled out every code-side cause (hook shipped, folder name matches manifest `id`, Sync toggle on, UI layer cannot swallow it) and landed on Sync's config-file cadence as the surviving hypothesis. See [the learning](docs/solutions/workflow-issues/a-sync-test-must-outwait-sync-before-it-can-blame-the-code.md) |
| 2026-08-06 | #347 #348 / #349 | A field and the button that commits it are one row — `formRow`, a **sixth** row kind, composed not configured; four pairs, 8 rows → 4; `accountInput` and the three `dataset.plus*` markers deleted. Back chevron moved to the row's leading edge in the **base** rule — desktop had the same 236px gutter, so it was never an `is-phone` fix · **0.6.83**. **Adversarial QA caught a regression worth remembering:** Obsidian stacks settings rows only under `is-phone`, so an `is-phone`-gated rule leaves `is-tablet` with neither — the merged inputs squeezed to 76–98px on iPad. Fixed with a 164px floor, not a second stacking gate. Follow-ups open: #350, #351 |
| 2026-08-06 | #342 #346 / #345 | Proposed tags stop renting a row each for Dismiss; one section-level **N proposals waiting / Dismiss all** row, scoped to the proposals it rendered · **0.6.82** |
| 2026-08-06 | #315 #314 / #329 | One egress disclosure for Home and Settings — a **union** of both strings, not a swap · **0.6.81** |
| 2026-08-06 | #334 / #335 | Field notes public archive on tryatoms (`/notes/`); www-only |
| 2026-08-06 | #331 / #332 | Ask `search_atoms` confidence + true empties (Fly deployed; no plugin bump) |
| 2026-08-06 | #325 / #326 | Root `npm test` + `npm run build` now run on `pull_request` (infra only, no plugin bump). **Not yet a required check** — branch protection on `master` still requires only `test` (plus-service), so `test + build` reports without blocking until an admin adds it |
| 2026-08-05 | #309 / #310 | One PKCE S256 digest, pinned to base64url (server-only, no plugin bump). **#240 must import `pkceChallengeS256` — never `hashToken`, which is hex.** Its plan said hex for `verifier_hash`; corrected on `feat/240-magic-link-handoff` (`a692e2f`, rides on #298) |
| 2026-08-05 | #287 / #297 | Library **Skipped** filter + long-press **Try filing…** (Reconsider; Plus/BYOK) · **0.6.77**. The Home path is deliberately **not** behind `enableReconsiderCapture` — #304's U8 removes the leftover flag |
| 2026-08-05 | #301 / #302 | Hero shows the loop closing — capture beat per storyline + promoted sub-line; verbatim match guarded by test (www-only) |
| 2026-08-05 | #299 / #300 | `/setup` installs from the community directory, not BRAT; adds the Open Atoms step; guide tightened 1582→1332 words (www-only, no plugin bump) |
| 2026-08-05 | #294 / #295 | Custom capture-shortcut link is optional, not a setup step · **0.6.76**. **Prepend, never append**, to `BUILTIN_INSTALL_URLS` when the link moves — the constant is its head |
| 2026-08-05 | #292 / #293 | Built-in capture shortcut points at the current iCloud link; shortcut CTA re-armed (2.1.0) · **0.6.75** |
| 2026-08-05 | #288 / #289 | Library tap=vault; long-press Continue · **0.6.73** |
| 2026-08-05 | #280 #282 / #281 | One "Atoms Plus is ready" notice per checkout, not one per in-flight poll; resume listeners tied to the plugin lifecycle · **0.6.72**. **#241 must land on top of this** — widening the poll window without the atomic announce is #280 again |
| 2026-08-05 | #277 / #278 | `/setup` from-zero guide (install · iOS capture · Ask); landing `#install` shrinks to a pointer. **Follow-up: screenshots need recapturing on desktop dark — the shipped ones are poor** |
| 2026-08-05 | #16 / #275 | Home Continue + pending parent handoff · **0.6.71** (community lint blockers) |
| 2026-08-05 | #238 / #268 | Stripe reconcile + ops alerts (server-only) |
| 2026-08-05 | #239 / #273 | First CI job that runs plus-service tests; real postgres coverage for the production store (test infra only, no plugin bump). Now a **required** check on `master` (see #238 tail) |
| 2026-08-05 | #257 #258 / #272 | Ask MCP `created` + list sort/filter + `list_pending` · **0.6.70** (Fly deploy still needed) |
| 2026-08-04 | #222 / #266 | Catch-up on resume + Sync everything now · **0.6.67** |
| 2026-08-04 | #256 / #262–#265 | Ask MCP `list_tags` + residuals · Fly live (no plugin bump until 0.6.67) |
| 2026-08-04 | #255 #259 / #260 | Ask MCP `mirror_status` + absence wording · Fly live (no plugin bump) |
| 2026-08-04 | #251 / #252 | Ask MCP pairing code + OAuth identity chooser · **0.6.66** (released; Fly already live) |
| 2026-08-04 | #246 / #247 | Ask coordinator peel + wire the inert confirm-withdrawal hook · **0.6.65** (released) |
| 2026-08-04 | #248 / #249 | Mirror completeness floor counts surviving evidence, not raw scan · **0.6.64** |
| 2026-08-04 | #225 / #226 | Gate mirror deletion, fix outbox ack, verify marker placement · **0.6.63** |
| 2026-08-04 | #243 / #244 | Classify dual-surface parity freeze + people hard-rules line · **0.6.63** |
| 2026-08-02 | #230 / #231 | Production trial checkout no longer revokes the plugin session; a rejected session is never a dead end · **0.6.62** |
| 2026-08-01 | #224 #227 / #228 | Person invites name model-identified people, not the leading verb · **0.6.61** |
| 2026-07-30 | #196 / #218 | Classify prompt same-thread link soften + quality 7 · **0.6.57** |
| 2026-07-30 | #186 / #187 | BM25 body-scored shortlists for classify context · **0.6.53** |
| 2026-07-30 | #206 / #207 | tryatoms Obsidian on-ramp for newcomers (install step 1 + hero link) |
| 2026-07-29 | #194 / #195 | Ask MCP Bar B — `atoms:write` scope + Claude directory pack (Fly already live) |
| 2026-07-29 | #188 / #189 | Ask MCP dual-era 2026-07-28 + OAuth `iss` + Claude JSON Accept fix (Fly already live) |
| 2026-07-29 | #184 | Capture Shortcut link refreshed + renamed **Capture Atom** everywhere · **0.6.51** |
| 2026-07-29 | #182 / #183 | Cloudflare Web Analytics through the tryatoms.app CSP + corrected privacy claims |
| 2026-07-28 | #181 | Verified iOS Shortcut link + the recipe that actually works · 0.6.50 (shipped in 0.6.51) |
| 2026-07-28 | #56 / #167 | iOS capture inbox + drain into dailies · **0.6.49** |
| 2026-07-28 | #174 / #175 | Community review lint (styles, confirm, types) · **0.6.47** |
| 2026-07-28 | #115 | Plus public launch ops — tryatoms domain, Resend, Stripe **live** on Fly |
| 2026-07-28 | #150 / #152 | Ask MCP unmisreadable shapes (revision status, snippet authority, scope) |
| 2026-07-27 | #120 / #146 | DIY Ask self-host guide (`docs/ask-self-host.md`) |
| 2026-07-27 | #119 / #141 | Ask ChatGPT MCP connector · **0.6.41** — Fly deploy + AE5 dogfood before BRAT |
| 2026-07-27 | #137 / #138 | Ask mirror parity (events + delete + reconcile) · **0.6.39** — Fly deploy + dogfood before BRAT release |
| 2026-07-27 | #131 #129 / #132 | Ask dogfood follow-ups · **0.6.36** |
| 2026-07-27 | #127 / #128 | Ask write path create/continue · **0.6.35** |
| 2026-07-27 | #123 / #124 | Plus stripe-first onboarding (0.6.33) |
| 2026-07-27 | #112 / #116 | Ask your brain remote MCP · release **0.6.32** |
| 2026-07-27 | #91 #107 / #92 | Atoms Plus managed filing + production backend (0.6.30) |
| 2026-07-22 | #100 / #102 | Reconsider capture (flagged) · release 0.6.27 |
| 2026-07-18 | #97 / #98 | Library row opens vault note (not in-home reader) |
| 2026-07-18 | #93 #95 / #94 | Entity orbits T0–T3 + Together Open list (0.6.26) |
| 2026-07-17 | #83 / #84 | Open atom graph command (0.6.19) |
| 2026-07-17 | #74 / #75 | ghost textButton kit + bridge chrome fix |
| 2026-07-17 | #71 / #72 | Land, then remember + drop For you label (0.6.14) |
| 2026-07-17 | #66 / #67 | collision body-gate + Process failure surface (0.6.13) |
| 2026-07-16 | #63 / #64 | library within-day created order (0.6.12) |
| 2026-07-16 | — | remove PR Closes CI; agent-checked only |
| 2026-07-16 | #53 / #54 | drop junk link reasons (0.6.10) |
| 2026-07-16 | #48 / #49 | aliases/prior as self (0.6.9) |
