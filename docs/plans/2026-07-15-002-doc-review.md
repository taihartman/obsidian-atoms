# Doc review: vault-aware people linking plan

**Document:** `docs/plans/2026-07-15-002-feat-vault-aware-people-linking-plan.md`  
**Date:** 2026-07-15  
**Type:** unified-plan (implementation-ready)  
**Origin:** none (greenfield)

## Coverage

| Persona | Findings (pre-merge) | Notes |
|---|---|---|
| coherence | 10 | |
| feasibility | 14 | codebase-grounded |
| product-lens | 13 | remapped invalid enums |
| security-lens | 7 | remapped invalid enums; 1 positive dropped |
| scope-guardian | 11 | |
| adversarial | 7 | |
| cross-model peers | — | skipped (no different provider run this session) |

## Applied fixes (mechanical)

1. Design principles: “Always-on structural tags” → “Structural tags always eligible” (not auto-applied every capture).
2. Path allowlist vs denylist wording split; denylist parenthetical removed from allowlist bullet.
3. Path strings normalized toward `/Social/`, `/People/`, `/Personal notes/` in risks + Decision 3.
4. Decision 3 rewritten as hybrid choice (not exclusive-OR path-only vs vault-wide).

## Merged actionable findings

### Decisions (manual)

| ID | Sev | Title | Reviewers | Rec |
|---|---|---|---|---|
| D1 | P0 | Lock three open decisions as KTDs; resolve implementation-ready vs “Decision needed” | coherence, feasibility, product, scope | Apply |
| D2 | P0 | Wire enrich on **one choke-point** (classify success + fixture write + backfill apply) | feasibility, scope | Apply |
| D3 | P1 | Drop or tightly gate **vault-wide** capitalised basename hubs (false-hub factory) | feasibility, product, scope, adversarial | Apply |
| D4 | P1 | **P3-first**: repair is main lever; treat full discovery+personHubs prefix as optional until measured | product, scope, adversarial | Apply |
| D5 | P1 | **Aboutness gate** for repair — not every atom name co-occurrence; avoid generic `about [[X]]` spam | product, adversarial, security | Apply |
| D6 | P1 | Spec **alias → canonical title** match keys for repair | feasibility, product, adversarial | Apply |
| D7 | P1 | Cut **preference-keyword** auto-tag from P3; model-only for `#preferences` | scope, product | Apply |
| D8 | P1 | Product proof: dry-run/Notice must surface people links (don’t rely only on Backlinks panel) | product | Apply |
| D9 | P2 | Zero-setup claim = title/alias match, not “infer who is a person in any vault layout” | product, adversarial | Defer |

### Proposed fixes (gated_auto)

| ID | Sev | Title | Reviewers | Rec |
|---|---|---|---|---|
| G1 | P1 | Reuse `collectLinkTargets` alias logic; cache-aware `discoverPersonHubs(files)` | feasibility | Apply |
| G2 | P1 | Rewrite Risks privacy row (no path egress; people-hub labeling is semantic amplification) | security | Apply |
| G3 | P2 | Clarify signal priority = inclusion/sort, not name resolution | coherence | Apply |
| G4 | P2 | Bind 1–3 word heuristic to path signal; keep vault-wide single-token only if vault-wide kept | coherence | Apply |
| G5 | P2 | Empty hubs: stable `(none)` section; enrich no-ops | feasibility | Apply |
| G6 | P2 | Tin role: call person hub if listed under Person hubs | coherence | Apply |
| G7 | P2 | Settings privacy copy mentions derived person-hub title list (P4) | security | Apply |

### FYI

- Duplicate basenames rare; collision policy optional (feasibility 50).
- Strategic CRM expectation pressure if success looks automatic (product).
- Logging: prefer counts over full hub lists outside dev (security).
- Architecture write path already matches plan (positive).

## Premise-dependency chains

**Root D4** (P3-first / simplify stack): if rejected (Skip), dependents D3 (vault-wide) and G3/G4 (priority/heuristic details) still matter independently — not cascaded.  
**Root D1** (lock decisions): if Skip, D3/D5 remain open product forks.

## Recommended product lock (if user picks best-judgment)

1. Repair **atom only**; no auto-create.  
2. Hubs = path allowlist `/Social/|/People/|/Personal notes/` only for v1 (drop vault-wide).  
3. Repair match keys = basename ∪ aliases → canonical basename; min length ≥3–4 or path-strong hubs only for short names.  
4. Repair: inject link + optional `#person` when hub matched on atom; **no** preference-keyword injection.  
5. Wire enrich inside `classifyCapture` success + backfill apply + fixtures.  
6. Ship repair against existing titles first; dedicated `personHubs` prefix only if dry-run still under-links.  
7. Dry-run/Notice must show person link outcomes.  
8. Privacy residual + Settings disclosure as above.

---

## Walk-through outcomes

| Finding | Action |
|---|---|
| D1 Lock three KTDs | **Applied** (user) — Settled decisions table |
| D2 Choke-point wiring | **Applied** then bulk |
| D3–D9, G1–G7 | **Bulk best-judgment applied** (user Proceed) |

**Plan document rewritten** to incorporate: path-only KTD-P3, P3-first unit order (repair as P2), PersonHub records + alias→canonical, person-shaped repair gate, no preference-keyword inject, privacy rewrite, empty hubs `(none)`, Tin as hub, verification + Notice proof, Open Questions deferred items.

**Completion:** Review complete. Plan is ready for user approval before `ce-work`.
