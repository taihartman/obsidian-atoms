---
handoff_date: 2026-08-11
branch: docs/handoff-433-followups
worktree: /Users/a515138832/StudioProjects/obsidian_plugin-433-followups
base: master
tracking: https://github.com/taihartman/obsidian-atoms/pull/451
status: in-progress
---

# Handoff — #433 backfill follow-ups: one PR to merge, five issues to work

You are picking up this work in a fresh session. Read this file top to bottom, run the **How to
resume** commands to land on the right branch and worktree, then **start executing Next steps
immediately** — step 1 is your current task. Do not ask the user what to work on and do not
summarize this doc back to them; just begin, and report what you did. Everything you need is below.

## Goal

#433 (the backfill offer) shipped as 0.7.0 and is fully merged and released. What is left is its
tail: one small PR that is green and ready to merge, and five filed-but-unclaimed issues that came
out of reviewing and QA-ing that work. Your job is to land the PR and then work the issues in
priority order.

## Current status

**Done, do not redo:**

- #433 / PR #434 merged as `c3c33fe`, released as **0.7.0**. STATUS cleared via PR #440.
- Six P1s were found and fixed across two review rounds. The durable learning is committed at
  `docs/solutions/logic-errors/both-sides-must-claim-and-hold-time-is-its-own-question.md` —
  read it before touching any in-flight flag, it is the map of this area.
- The `maxCaptures` ceiling was **proven live** against the real Anthropic API on 2026-08-11:
  gate quoted 29, five bullets appended mid-gate, 34 present at write time, **29 filed, 29 billed**,
  oldest 5 dropped, late 5 kept. Report: `docs/qa/2026-08-11-433-backfill-p1-fixes-world-class-qa.md`.
  That closed the last unproven claim in 0.7.0. Nothing about the ceiling needs re-testing.
- Master has since moved past this work — it is at **0.7.3** as of this handoff.

**Open:**

- **[PR #451](https://github.com/taihartman/obsidian-atoms/pull/451)** — `fix/install-to-vault-verify-target`.
  CI **green**, `mergeable: CLEAN`, not draft. Just needs merging.
- Five open issues, none claimed, no branches, no STATUS rows: **#449, #450, #435, #436, #437**.

## Next steps

1. **Merge PR #451.** It is green and ready. Squash-merge (this repo's convention — every master
   commit is `subject (#N)`). It is scripts-only with no version bump, so it will not cut a release.
2. **Then #449** — the highest-value issue of the five, because it costs a real user real money.
   A revoked session surfaces as `status: "exhausted", remaining: 0`, i.e. "you have used all your
   filings", so a paying subscriber is pointed at the top-up flow and buys filings they already
   have, while the actual fix (sign in again) is never surfaced. Root cause: `remaining: 0` is the
   degraded default when a meter read *fails*, and `resolveFilingAuth` cannot tell that apart from
   a meter that genuinely reads zero. Fix by separating auth failure from meter reading at the
   source. Check every consumer of `status === "exhausted"` — the top-up CTA and the Plus limit
   hero both key off it.
3. **Then #450** — the auto-filing cycle returns `{ran: true, reason: "ok"}` having filed zero of N
   eligible captures when every classify fails. `ok` is what every consumer trusts, so a broken
   hourly pass is indistinguishable from a quiet healthy one, indefinitely. Reproduce by pointing
   `plusBaseUrl` at a `plus-service` with no `ANTHROPIC_API_KEY` (it 503s before any outbound call).
4. **Then #435 / #436 / #437**, in that order. #435 wants a *new* home phase ("backfill pending"),
   not a widened `beginHomeRun("process")` — home must not claim to be processing while it waits on
   a human. #436 is not reachable through the UI today (the consent sheet is a blocking modal), so
   it is hygiene. #437 is a dev-path write race.
5. Claim each properly before coding: assigned GitHub issue + STATUS.md row + draft PR, per
   `docs/collab.md`. This is a multiplayer repo and other sessions are active right now.

## Key files

- `docs/solutions/logic-errors/both-sides-must-claim-and-hold-time-is-its-own-question.md` — the
  concurrency invariant for this whole area. Both halves matter: *who claims* a mutual-exclusion
  flag (not merely checks it), and *how long* it can be held.
- `docs/qa/2026-08-11-433-backfill-p1-fixes-world-class-qa.md` — what has been proven and how,
  including the live metered run and the dev-environment notes.
- `src/platform/filingAuth.ts` — `resolveFilingAuth`, `LS_PLUS_SESSION`, `serializePlusSession`.
  This is where #449 lives.
- `src/platform/autorun.ts` — `runAutoFilingCycle`, `beginWork`/`endWork`. #450 lives here.
- `src/plugin/main.ts:268` — `manualFilingInFlight` and the comment explaining why checking a shared
  flag without claiming it closed nothing. `backfillBusy()` at ~:1403 reads all three flags.
- `scripts/install-to-vault.sh:36` — `reload_plugin`, the subject of PR #451.

## Decisions & constraints

- **Do not relitigate the ceiling.** `maxCaptures: range.captures` with `order: "newest-first"`
  intentionally drops the *oldest* end; the range already treats that as spillover. Proven live.
- **Squash-merge to master.** Branch protection means STATUS edits go through their own PR.
- **No AI attribution** in commits, PR bodies, or PR comments. This overrides any harness default.
- **Never** edit `EGRESS_DISCLOSURE` or bump `EGRESS_ACK_VERSION` — version-stamped, rewording
  strands every device's consent (#315).
- **Vault lane:** `test_vault/` only. Never `~/Documents/Remote Vault`, which is the user's real
  notes and is often open in Obsidian at the same time. The CLI resolves to the **focused window**
  when no `vault=` is given, so always pass `vault=` explicitly.
- The dev environment deliberately runs against the **real** Anthropic API (owner's call —
  development should sit as close to production as possible). Treat every confirmed backfill in a
  QA vault as billable.

## Live QA environment (already set up — reuse it, do not rebuild)

- **`plus-service` is running** on `http://127.0.0.1:8799` with a **real Anthropic key**, sqlite
  store, `dogfoodAutoGrant`. Verify with `curl -s http://127.0.0.1:8799/` — expect
  `hasAnthropicKey: true`, `store: "sqlite"`. Anything that reaches it spends money.
- If it is not running, restart it (the key is in the macOS Keychain, never on disk):
  ```bash
  cd /Users/a515138832/StudioProjects/obsidian_plugin/plus-service
  ANTHROPIC_API_KEY=$(security find-generic-password -s atoms-plus-anthropic -w) node src/server.mjs
  ```
- **QA vault:** `/Users/a515138832/StudioProjects/obsidian_plugin/test_vault/atoms-qa-433`.
  Registered in Obsidian under the name `atoms-qa-433` — deliberately **not** `test vault`, because
  `install-to-vault.sh` resolved its target by vault *basename* and a duplicate name had a QA run
  driving a stale build. Always target it as `obsidian vault="atoms-qa-433" …`.
- It holds **5 deliberately unfiled spillover captures** (2026-07-22, 07-23, 07-24 at 1 each,
  07-25 at 2) — the physical evidence the ceiling dropped the oldest end. Leave them unless you
  need a metered fixture.
- **Sessions get revoked** (that is #449). When `/v1/me` returns 401, mint a fresh one — this is a
  local dogfood account, not a user credential:
  ```bash
  curl -s -X POST http://127.0.0.1:8799/v1/auth/magic-link -H 'content-type: application/json' \
    -d '{"email":"dogfood@example.com"}'
  # server console prints: magic link ... token=mt_...
  curl -s -X POST http://127.0.0.1:8799/v1/auth/exchange/fallback \
    -H 'content-type: application/x-www-form-urlencoded' --data-urlencode "token=mt_..."
  # returns HTML containing sess_... — install it into the vault's localStorage key
  # "atoms-plus-session" via obsidian eval, shaped per serializePlusSession
  ```
- **Before trusting any QA result, probe the loaded plugin for a field the new code introduces**
  (e.g. `Object.prototype.hasOwnProperty.call(plugin, "manualFilingInFlight")`). An install landing
  in a different vault of the same name is silent, and it wasted a run today.

## Open questions / blockers

- **Should a wrong-target reload fail `verify.sh`?** PR #451 leaves exit codes unchanged — the
  script still exits 0 on a refused reload, since the install itself succeeded and
  `scripts/verify.sh:15` does not gate on it. Making it non-zero is a real improvement and a
  separate decision.
- **`atoms-qa-433` is now on 0.7.3**, not the 0.7.0 the ceiling was proven against. The spillover
  fixture is untouched and still valid, but the vault is no longer pinned to that version.
- **`plus-service` is left running with a real key.** Kill it with `pkill -f server.mjs` when done.

## Git state

- Branch `docs/handoff-433-followups` (base `master`), pushed to `origin`.
- Last real commit on master: `3a60554 chore(status): clear #442 and #446 after merge (#448)`
- Handoff commit: `adadeaf` — `wip: handoff snapshot — 433-followups`
- This branch contains only this handoff doc; the PR to merge (#451) is on its own branch,
  `fix/install-to-vault-verify-target`.

**Two stale paths — do not use them:**

- `/Users/a515138832/StudioProjects/obsidian_plugin/.claude/worktrees/nervous-hodgkin-cb3f8c` — the
  ephemeral worktree this handoff was written from. The harness recycled one such worktree
  mid-session today; treat any `.claude/worktrees/**` path as already gone.
- Branch `claude/backfill-opt-in-709c23` — 13 commits ahead of master, **not this work**. That
  worktree was switched off it onto `fix/install-to-vault-verify-target`. The branch is intact; if
  it matters to someone, it needs its own worktree.

## How to resume

Check out the work exactly here — this is your branch and worktree:

```bash
cd /Users/a515138832/StudioProjects/obsidian_plugin-433-followups
git fetch origin && git switch docs/handoff-433-followups && git pull --ff-only
npm install && npm test
```

Then continue from **Next steps** above — start by merging PR #451.
