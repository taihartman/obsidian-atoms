---
handoff_date: 2026-07-27
branch: docs/ask-self-host-120
worktree: /Users/a515138832/StudioProjects/obsidian_plugin-ask-self-host-120
base: master
tracking: https://github.com/taihartman/obsidian-atoms/issues/120
status: in-progress
---

# Handoff — #120 DIY Ask self-host docs

You are picking up this work in a fresh session. Read this file top to bottom, run the **How to resume** commands to land on the right branch and worktree, then **start executing Next steps immediately** — step 1 is your current task. Do not ask the user what to work on and do not summarize this doc back to them; just begin, and report what you did. Everything you need is below.

## Goal

Complete `docs/ask-self-host.md` for free/DIY users (Issue #120): honest, code-grounded guide to self-host **plus-service** + plugin URL override + OAuth MCP. Hosted Plus remains primary. **Do not invent** authless/static-bearer/live-folder MCP — those are not in code.

## Current status

- Issue **#120** assigned to user (`@me` via gh); title: *docs: Ask brain DIY self-host guide (P2)*.
- Branch **`docs/ask-self-host-120`** cut from master (clean at handoff start).
- **No doc rewrite yet** — only research. Stub still says incomplete/P2.
- Code reality surveyed (session):
  - MCP tools read **DB mirror only** (`plus-service/src/mcp/tools.mjs`) — never live vault `Atoms/` FS.
  - Auth: OAuth magic-link → `mcp_*` only; unauth `/mcp` = 401; **no** static bearer / authless.
  - DIY free of hosted bill = run `plus-service` yourself + `DOGFOOD_AUTO_GRANT=1` + plugin **Plus service URL override**.
  - Plugin: `plusBaseUrl` in settings (Dev section); `askMcpUrl(base)` → `{base}/mcp`.
  - Phone Claude needs public HTTPS `PUBLIC_BASE_URL` matching tunnel.
  - Plan KTD14 (folder + static bearer) is **intent only**, not implemented.
- Prior Ask arc shipped on master: mirror parity 0.6.39, ChatGPT connector 0.6.41, STATUS clear for those.

## Next steps

1. **Claim in STATUS.md** — row for #120 / this branch / hot file `docs/ask-self-host.md` (State: In progress).
2. **Rewrite `docs/ask-self-host.md`** using the outline below (code-grounded; no vaporware).
3. **Wire Settings** — Self-host Ask button already opens this doc URL; update button desc from “incomplete (P2)” to accurate if doc is complete; ensure GitHub blob path still works on master after merge.
4. **Light cross-links** — `plus-service/README.md`, `docs/runbooks/atoms-plus-prod.md`, `CONCEPTS.md` Remote MCP one-liner if needed; keep `docs/dev-obsidian-mcp.md` explicitly NOT this product.
5. **Draft PR** with `Closes #120`, docs-only evidence N/A, push branch.
6. Do **not** implement authless/folder FS backend unless user expands scope — that is a feature, not this issue.

### Doc outline (authoritative for step 2)

```text
# Self-host Atoms Ask (DIY)
## Status — what works today vs hosted Plus
## What this is / is not
  IS: self-host plus-service; plugin URL override; dogfood entitlement; OAuth MCP
  IS NOT: authless, static bearer, live Atoms/ folder backend, free tryatoms.app
## Architecture (short)
  Obsidian sess_ → mirror HTTP → DB
  Claude/ChatGPT mcp_ OAuth → /mcp → store.mirror*
## Prerequisites
## Minimal local run (env table + npm start)
## Plugin wiring (Plus URL override, sess_, Ask enable, Sync now, copy MCP URL)
## Public tunnel for phone Claude (PUBLIC_BASE_URL = tunnel HTTPS)
## Optional: npm run ask:seed
## Auth model (sess_ vs mcp_; active|trialing)
## Production self-host (prodGate) — advanced, not free DIY
## Security / privacy / wipe
## NOT supported checklist
## Troubleshooting
## Related links + Issue #120
```

Env keys that matter: `PORT`, `PUBLIC_BASE_URL`, `DOGFOOD_AUTO_GRANT`, `ATOMS_PLUS_STORE`/`ATOMS_PLUS_DATABASE_PATH`, optional `ATOMS_ASK_MIRROR_KEY`. OAuth allowlist already includes Claude + ChatGPT + loopback (master).

## Key files

- `docs/ask-self-host.md` — rewrite target (stub today)
- `src/settings/settings.ts` — Self-host Ask button + Plus URL override + Ask section
- `src/platform/plusClient.ts` — `DEFAULT_PLUS_BASE_URL`, `askMcpUrl`
- `plus-service/src/mcp/handler.mjs` — MCP auth
- `plus-service/src/mcp/tools.mjs` — mirror-only tools
- `plus-service/src/config.mjs` / `prodGate.mjs` — env + production gate
- `plus-service/README.md` / `.env.example` — local run patterns
- `docs/plans/2026-07-27-001-feat-ask-brain-remote-mcp-plan.md` — KTD14 intent (do not claim as shipped)
- `docs/dev-obsidian-mcp.md` — agent Local REST only; call out distinction

## Decisions & constraints

- **Docs-only scope for #120** unless user says otherwise.
- Honest: no live folder MCP, no authless/static bearer in code.
- Body sacred / vault SSOT / agents never unattended personal vault — dogfood on `test_vault/` only if any smoke.
- Constitution docs already mention Claude+ChatGPT same URL; DIY is self-host of that stack.
- Do not cut GitHub Release unless asked.
- PR must `Closes #120`; clear STATUS on merge.

## Open questions / blockers

- none blocking docs — if user wants real folder/authless backend, that is a new feature issue, not #120.

## Git state

- Branch `docs/ask-self-host-120` (base `master`), pushed to `origin`.
- Last real commit on master at branch cut: `32a8f79` Merge PR #143 status clear #119
- WIP snapshot commit: `5bf0c14` — `wip: handoff snapshot — ask-self-host-120`
- Diff since base:  2 files changed, 110 insertions(+)

## How to resume

Check out the work exactly here — this is your branch and worktree:

```bash
cd /Users/a515138832/StudioProjects/obsidian_plugin-ask-self-host-120
git fetch origin && git switch docs/ask-self-host-120 && git pull --ff-only
# docs-only — no npm required to start writing
```

Then continue from **Next steps** above.
