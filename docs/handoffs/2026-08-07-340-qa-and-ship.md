---
handoff_date: 2026-08-07
branch: feat/339-ask-search-recall-honesty
worktree: /Users/a515138832/StudioProjects/obsidian_plugin-340-ask-expand
base: master
tracking: https://github.com/taihartman/obsidian-atoms/issues/339 · PR https://github.com/taihartman/obsidian-atoms/pull/340
status: in-progress
---

# Handoff — #340: QA the consent re-prompt, then ship

Read this top to bottom, run **How to resume**, then start at Next steps. Do not re-plan what is decided
below and do not summarize this doc back to the owner — just work and report.

## Where this stands

PR #340 is **open, MERGEABLE, not draft**, plugin **0.6.87**. Everything from the previous handoff's
four-step path is done: master merged twice, `ASK_PRIVACY_ACK_VERSION` bumped to `2026-08-07` with a new
keyed `FROZEN_ASK_PRIVACY` entry, and `ASK_EXPAND_ENABLED` flipped **on by default** on the owner's explicit
call. A full branch code review ran (10 reviewers + a validator batch); its P1s are fixed and committed.

**Both suites green:** root `npx vitest run` 1399 pass / 81 files; `plus-service` `node --test test/*.test.mjs`
512 pass. `npx tsc --noEmit` clean.

**The only thing standing between this and merge is `world-class-qa`.** That is Next step 1.

## Read these first

- Code review report (not committed, session-scoped — read it before QA so you know where the bodies are):
  `/tmp/compound-engineering-502/ce-code-review/20260807-132859-d95248de/report.md`, with per-reviewer JSON
  artifacts beside it. If that tmp dir is gone, the findings that still matter are all reflected in the PR
  body and in the two solutions docs below.
- `docs/solutions/security/a-consent-version-only-the-client-checks-does-not-gate-the-server.md` — the
  accepted risk, written up.
- `docs/solutions/best-practices/a-source-file-with-nul-bytes-is-invisible-to-code-review.md`.

## Decided — do not reopen

1. **`ASK_EXPAND_ENABLED` defaults to `"1"` (on).** Owner's call. The F3 guardrail in
   `plus-service/test/ask-expand-guardrails.test.mjs` was rewritten to pin what survived the flip — an
   operator off switch and inertness with no `ANTHROPIC_API_KEY`. Do not flip it back.
2. **The client-side-only consent gate is an ACCEPTED RISK, not a bug to fix.** A device on 0.6.86 keeps
   mirroring and the server expands its bodies under the old six-clause wording, retroactively for rows
   already stored. The owner accepted this on the expectation that users update. It is written up in the PR
   body under "Known accepted risk" and in the solutions doc. **Do not re-litigate it and do not quietly
   implement a server-side gate** — if you think it should change, say so and stop.
3. **KTD6's token-overlap clause is withdrawn.** It requires a generated phrase to share a >=4-char token
   with the note's own text, which is definitionally what a paraphrase lacks. It shipped, was reverted in
   `8c367b7` for killing recall in production, was re-implemented by mistake during this review, and was
   reverted again in `1df9b60`. A dated correction sits under KTD6 in the plan. **If a future reviewer tells
   you this guard "is missing", it is missing on purpose** — point them at the correction note.
4. Never `npm test` or `npm run build` (they delete `docs/field-notes/published/2026-08-01-sample-loop.json`,
   issue #343). Use `npx vitest run` and `node --test test/*.test.mjs`.
5. Vault lane: `test vault` only. Never `~/Documents/Remote Vault`.
6. No AI attribution in commits, PR bodies, or review replies.
7. Work in this worktree. The main checkout at `/Users/a515138832/StudioProjects/obsidian_plugin` belongs to
   the owner.

## Next steps

1. **Run `world-class-qa`, ending in `adversarial-qa`.** This is the merge gate and the reason this handoff
   exists. The changed user-facing surface is the **Ask consent re-prompt**: on 0.6.87 every existing device
   must re-accept, and *both* Ask consents re-prompt because `settleAckRecords` clears the write ack when the
   privacy ack goes stale (deliberate, from #360 — not a bug you found).
   Prove at least: a device holding the `2026-08-06` ack is re-prompted; accepting stamps `2026-08-07`;
   declining leaves the mirror off; the seven-clause wording actually renders, with clause (4) naming the
   Anthropic egress; and `Settings -> Atoms` shows `Version 0.6.87`.
   Commit screenshots under `docs/qa/screenshots/340-ask-expand/` and write the report to
   `docs/qa/2026-08-07-340-ask-expand-world-class-qa.md`.

2. **Fill in the PR body's Evidence section and tick the two open Test plan boxes** — the live-vault QA box
   and the evidence line both currently say "not yet captured", honestly. Link screenshots with **absolute**
   `https://raw.githubusercontent.com/taihartman/obsidian-atoms/feat/339-ask-search-recall-honesty/...` URLs;
   repo-relative paths render as broken images in PR descriptions.

3. **Merge is the owner's call, and it now ships a Release.** `beab58f` landed auto-publish on a master
   version bump, so merging 0.6.87 pushes it to BRAT users with no separate step. Say that out loud when you
   hand back — it is new since the last handoff, and it interacts with decision 2 above: faster propagation is
   exactly the mitigation the accepted risk leans on.

4. **`plus-service` still needs a Fly deploy** or expansion does nothing in production, whatever the flag says.
   Egress is double-gated on `config.anthropicApiKey`, so a deploy without that key is inert. Separate act
   from merging; the owner drives it.

5. **After merge:** clear `STATUS.md`. Do not cut a Release by hand — it is automatic now.

## Deliberately not done

- **Review finding #9** — `search_atoms` runs a second full-account scan per call (`mirrorExpandCoverage`)
  when `mirrorSearch` already has the rows in memory. Real, bounded at current vault sizes, worth a follow-up
  issue rather than a scope-widening fix here.
- The `searched_fields` / `retrieval` payload inconsistency the agent-native reviewer found:
  `SEARCHED_FIELDS` is still `["title","tags","body"]` (`askHelpers.mjs:935`) while a hit in the same payload
  can carry `match_signals: ["expand"]`. The payload disagrees with itself. Small, and it deserves its own issue.
- Pre-existing and untouched: the KTD3 relative-floor comparison; expand store methods have memory-store
  coverage only, no sqlite/postgres.

## Cross-model review note

The Grok adversarial peer produced **nothing** on this run — it burned its 600s hard-only window reading a
3,105-line diff and `--json-schema` buffers, so a run that dies before the final step yields literally zero.
The adversarial lens is recorded as **degraded** in the review Coverage. Do not "fix" this by raising
`CROSS_MODEL_HARD_SECS`; it is one knob feeding every route and converts grok's bound into a 1200s window with
no idle detector. The lever is giving the peer less to read — stage a 2-3 file diff rather than the branch.
The owner raised this mid-session; nothing was changed.

## Git state

- Branch `feat/339-ask-search-recall-honesty`, pushed, upstream set. HEAD `d0c1ddf`.
- This session's commits: `39bfde5` merge, `89bb5f5` ack version bump, `cd1a742` flag flip,
  `4f70698` simplify pass, `0526ff3` review fixes, `65d2dd6` two solutions docs,
  `1df9b60` KTD6 revert, `d0c1ddf` merge master.
- Tracked tree clean at handoff time.

## How to resume

```bash
cd /Users/a515138832/StudioProjects/obsidian_plugin-340-ask-expand
git fetch origin && git switch feat/339-ask-search-recall-honesty && git pull --ff-only
npm install
npx vitest run
```

Then start at Next step 1.
